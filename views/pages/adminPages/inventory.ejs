<!DOCTYPE html>
<html>

<head>
  <% include ../../includes/header.ejs %>
  <link rel="stylesheet" type="text/css" href="/css/inventory.css" /> 
</head>

<body>
  <% include ../../includes/nav.ejs %>
  <main>
    <% if (prods.length > 0) { %>
        <table class="table table-striped table-outlined table-hover">
          <thead>
            <tr>
              <% Object.keys(prods[0]).forEach(function(key) { %>
                <% if(key == '_id'){ %>
                <th class="tbl-actions">
                  <input id="check-all" type="checkbox" /> 
                  <a class="link" href="/adminPages/admin/add-product"><i class="fa fa-plus"></i></a>
                  <!--<a class="link" href="/admin/edit-products-batch"><i class="fa fa-edit"></i></a>-->
                  <a class="link" href="/admin/copy-products-batch"><i class="fa fa-copy"></i></a>
                  <a class="link" href="/admin/delete-products-batch"><i class="fa fa-trash"></i></a>
                </th>
                <% } else { %>
                <th><%= key %></th>
                <% } %>
              <% }); %>
            </tr>
          </thead>
          <tbody>
            <% for (let product of prods) { %>
              <tr data-id="<%= product._id %>">
                  <% Object.keys(product).forEach(function(key) { %>
                    <% switch(key){ 
                        case '_id': %>
                          <td data-id="<%= product._id %>" class="row-actions">
                            <div class="btn-group">
                              <input class="row-check" type="checkbox" data-id="<%= product._id %>" />
                              <a class="link" href="<%= '/adminPages/admin/edit-product/' + product._id + '?edit=true' %>"><i class="fa fa-edit"></i></a>
                              <a class="link" href="<%= '/adminPages/admin/copy-product?productId=' + product._id %>"><i class="fa fa-copy"></i></a>
                              <a class="link" href="<%= '/adminPages/admin/delete-product?productId=' + product._id %>"><i class="fa fa-trash"></i></a>
                            </div>
                          </td>
                          <% break;  
                        case 'rating':
                      %>
                        <td><%= JSON.stringify(product[key]) %></td>
                      <% break;  
                        case 'image': %>
                        <td><img alt="<%= product.title %>" class="tbl-img" src="<%= product[key] %>" /></td>
                        <% break; 
                        default:  %>
                          <td><%= product[key] %></td>
                        <% break;  %>
                      <% } %>
                  <% }); %>  
              </tr>                      
            <% } %>
          </tbody>
        </table>
    <% } else { %>
        <h1>No Products Found!</h1>
    <% } %>
</main>

<script type="text/javascript" src="/scripts/inventory.js"></script>
    <% include ../../includes/end.ejs %>